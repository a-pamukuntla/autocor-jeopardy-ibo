{
  "finalJeopardy": {
    "q": "Match each automation scenario to the best command or method",
    "a": "PATCH|send_config_set()|git revert|pyats diff|mcp.run('stdio')",
    "pairs": [
      {
        "id": 0,
        "term": "PATCH",
        "def": "Update only one field of an existing RESTCONF resource without replacing the full object"
      },
      {
        "id": 1,
        "term": "send_config_set()",
        "def": "Push multiple CLI configuration lines in config mode as one operation"
      },
      {
        "id": 2,
        "term": "git revert",
        "def": "Undo a bad commit in a shared branch by creating a new inverse commit"
      },
      {
        "id": 3,
        "term": "pyats diff",
        "def": "Compare pre-change and post-change snapshots to find operational state differences"
      },
      {
        "id": 4,
        "term": "mcp.run('stdio')",
        "def": "Start a FastMCP server over standard input/output for host-client communication"
      }
    ]
  },
  "categories": [
    {
      "name": "Network Automation",
      "questions": [
        {
          "q": "A network engineer needs to update only the description field of an existing interface resource without replacing the entire config. Which HTTP method should the RESTCONF request use?.",
          "a": "What is PATCH?"
        },
        {
          "q": "You need to apply a list of interface configuration commands in config mode and ensure they're all sent as a single transaction. Which Netmiko method fits this scenario?<pre><code>commands = [\n  'interface GigabitEthernet1',\n  'description WAN-LINK',\n  'no shutdown'\n]\nnet_connect.___________( commands )</code></pre>",
          "a": "What is send_config_set()?"
        },
        {
          "q": "When building this NETCONF payload to configure a VPN from a YANG tree, what operation attribute tells the device to create the VRF only if it doesn't already exist?<pre><code>&lt;config&gt;\n  &lt;native xmlns=\"...native\"&gt;\n    &lt;vrf operation=\"___________\"&gt;\n      &lt;definition&gt;\n        &lt;name&gt;VPN-CORP&lt;/name&gt;\n      &lt;/definition&gt;\n    &lt;/vrf&gt;\n  &lt;/native&gt;\n&lt;/config&gt;</code></pre>",
          "a": "What is merge?"
        },
        {
          "q": "This Ansible command-line flag, often used alongside check mode, provides a side-by-side comparison of the current device configuration versus the proposed changes before they are committed to the network device.",
          "a": "What is --diff?"
        },
        {
          "q": "You are tasked with moving a specific OSPF configuration resource from one Terraform module to another. To avoid a Destroy and Recreate action, you must use this command to re-map the existing physical resource to its new logical address in the code.",
          "a": "What is terraform state mv?"
        }
      ]
    },
    {
      "name": "Infrastructure as Code",
      "questions": [
        {
          "q": "An engineer needs to undo a commit that has already been pushed to a shared remote repository. To avoid rewriting history and causing issues for other team members, they use this command to create a new 'inverse' commit.",
          "a": "What is git revert?"
        },
        {
          "q": "A business requirement states that the automation system must never 'guess' the status of the network; it must only push configurations that match a central database. In this architecture, the database is serving as the...",
          "a": "What is the Source of Truth (SoT)?"
        },
        {
          "q": "What Terraform command would you run first to see what changes this config will make before applying it?<pre><code>resource \"iosxe_restconf\" \"ospf\" {\n  path = \"Cisco-IOS-XE-native:native/router/ospf\"\n  attributes = jsonencode({\n    \"Cisco-IOS-XE-ospf:process-id\" = 1\n  })\n}</code></pre>",
          "a": "What is terraform plan?"
        },
        {
          "q": "In a GitLab CI/CD pipeline, this specific stage (2.3.d) is responsible for running operational state checks (e.g., 'show ip ospf neighbor') to confirm that the network reached the desired state after the configuration was applied.",
          "a": "What is the post-validation stage?"
        },
        {
          "q": "In this Terraform snippet, what is the correct syntax to reference the output of one resource inside another?<pre><code>resource \"iosxe_interface\" \"loopback\" {\n  name = \"Loopback0\"\n}\n\nresource \"iosxe_restconf\" \"router_id\" {\n  attributes = jsonencode({\n    id = ___________\n  })\n}</code></pre>",
          "a": "What is iosxe_interface.loopback.name?"
        }
      ]
    },
    {
      "name": "Operations",
      "questions": [
        {
          "q": "In a Model-Driven Telemetry 'Dial-In' session, where the collector initiates the connection and the network device acts as the server, this XML-based protocol is used to establish and manage the subscription.",
          "a": "What is NETCONF?"
        },
        {
          "q": "In a Python automation script, this specific class from the 'logging.handlers' module is used to direct log messages to a remote server using the standard Syslog protocol, typically over UDP port 514.",
          "a": "What is SysLogHandler?"
        },
        {
          "q": "To prevent a Python script from attempting to configure an invalid network segment, an engineer implements this specific logic to ensure that a user-provided VLAN ID is an integer strictly within the range of 1 to 4094.",
          "a": "What is Input Validation?"
        },
        {
          "q": "After a configuration change, an engineer has two snapshots of the network state. To programmatically identify exactly which OSPF neighbors were lost or which routes were modified, they would execute this specific pyATS CLI tool.",
          "a": "What is pyats diff?"
        },
        {
          "q": "To ensure 'Non-Repudiation' and security in a network automation solution, an engineer configures the system to use these specific digital credentials, which are signed by a trusted third party to verify the identity of the automation server.",
          "a": "What are CA-Signed Certificates?"
        }
      ]
    },
    {
      "name": "AI in Automation",
      "questions": [
        {
          "q": "When an AI agent provides a response that is grammatically correct and technically plausible but contains non-existent CLI commands or incorrect logic, the recommendation is suffering from this specific phenomenon.",
          "a": "What is a Hallucination?"
        },
        {
          "q": "Looking at this FastMCP server skeleton, what is the correct transport argument to pass to run() so the server communicates over standard input/output with an MCP client?<pre><code>from fastmcp import FastMCP\nmcp = FastMCP('CiscoBot')\n\n@mcp.tool()\ndef get_bgp_summary(): ...\n\nif __name__ == '__main__':\n    mcp.run(transport='___________')</code></pre>",
          "a": "What is stdio?"
        },
        {
          "q": "In LangChain, what naming convention do all chat model wrapper classes follow, and what method is used to send a prompt and get a response?<pre><code>from langchain_openai import ___________\n\nllm = ___________(model='gpt-4o')\nresponse = llm.___________([HumanMessage(content='Explain OSPF')])</code></pre>",
          "a": "What is Chat[ModelName] (e.g. ChatOpenAI) and .invoke()?"
        },
        {
          "q": "This decorator in FastMCP defines a reusable prompt template so an AI agent follows a fixed troubleshooting sequence, such as diagnosing CRC errors step by step.<pre><code>___________\ndef crc_troubleshoot(interface: str) -> str:\n    return f\"Check interface {interface}, then check cables...\"</code></pre>",
          "a": "What is @mcp.prompt()?"
        },
        {
          "q": "This is the specific method called at the end of a Python script to actually start the FastMCP server and begin listening for JSON-RPC messages from the host.",
          "a": "What is mcp.run()?"
        }
      ]
    }
  ]
}